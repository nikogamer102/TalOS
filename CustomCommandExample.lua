local Settings = {
	perms = "everyone"
}


return function(Functions, args)
	print(args[1]) --print {text here}
end

--Reminder that its ModuleScript (There is same code inside customcommands folder)

--Name the modulescript to the name of command.
